let lapsCompleted = 0;

// Create a function that increments the lapsCompleted variable with one
// Run it three times

function lapCounter() {
  lapsCompleted += 1;
  console.log(lapsCompleted);
}


lapCounter();
lapCounter();
lapCounter();
